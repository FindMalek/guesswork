# Copy to .env.local and fill in your value. .env.local is gitignored --
# never commit a real key.

# -----------------------------------------------------------------------------
# AI (terminal playground demo, /api/suggest)
# -----------------------------------------------------------------------------
# TypeSafe API key -- the real Jev model, same one the zsh plugin uses.
# Get one at https://typesafe.ai. Optional: without it, the route handler
# falls back to the local deterministic demo (see src/lib/fake-rank.ts)
# instead of failing the request.
TYPESAFE_API_KEY=""
